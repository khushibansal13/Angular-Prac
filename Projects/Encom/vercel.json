{
  "version": 2,
  "builds": [
    {
      "src": "Projects/Encom/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/encom/browser",
        "skipCache": true
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
